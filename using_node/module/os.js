const os = require('os');

console.log(os.platform())// => gives the os platform
console.log(os.arch())// => gives the os architeture
console.log(os.cpus())// => gives the numbers of core
 
 console.log(os.homedir())// => gives the home directory of the current user 
